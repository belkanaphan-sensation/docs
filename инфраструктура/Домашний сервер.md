# Сервер из компьютера

## 1. Отключение графического интерфейса (опционально)

Если GUI не нужен, можно отключить автозагрузку графики:

```bash
sudo systemctl set-default multi-user.target
```

Теперь всегда будет загружаться консоль (SSH доступен).

Для включения GUI обратно:

```bash
sudo systemctl set-default graphical.target
```

## 2. Настройка SSH доступа

### На сервере (Linux)

```bash
sudo apt update 
sudo apt install -y openssh-server
sudo systemctl enable --now ssh
sudo systemctl start ssh
```

### Подключение с клиента

На Mac подключаться так:

```bash
ssh <user>@<ip-адрес-linux>
```

## 3. Определение IP-адреса

### Локальная сеть

Если оба компьютера в одной сети (Wi-Fi/роутер), используй локальный IP Linux-машины:

```bash
ip addr show
# или
ip a
```

Обычно это что-то вроде `192.168.0.50`.

### Доступ из интернета

Если хочешь подключаться из интернета (не только дома):

- У провайдера должен быть **белый IP** (или хотя бы динамический, тогда нужен DDNS)
- На роутере настроить **port forwarding**: пробросить порт `22` на Linux-комп
- Тогда сможешь делать `ssh user@<public-ip>` с любого места

## 4. Настройка удобств

### SSH-ключи

Чтобы не вводить пароль каждый раз, сгенерируй SSH-ключ на Mac:

```bash
ssh-keygen -t ed25519
ssh-copy-id user@192.168.0.50
```

### Настройка SSH config

Можно настроить `~/.ssh/config` на Mac:

```
Host myserver
    HostName 192.168.0.50
    User user
```

Тогда заходишь просто:

```bash
ssh myserver
```

## 5. Следующие шаги

Когда SSH доступ есть:

- Ставишь на Linux-компе `k3s`, `helm`, `flux` → он становится полноценным кластером
- С Mac можно управлять кластером через `kubectl` двумя способами:
    1. Работать по SSH прямо на Linux-сервере (самый простой)
    2. Скопировать `~/.kube/config` с Linux на Mac и использовать `kubectl` локально

---

# Доступ без белого IP

## 4 способа организации доступа

### 1. Ngrok / LocalTunnel / Cloudflare Tunnel (самый простой)

- Эти сервисы пробрасывают туннель от твоего локального сервера в интернет
- Ты получаешь публичный URL (например, `https://myapp.ngrok.io`), который можешь отдать напарнику
- Удобно для разработки, но не всегда подходит для продакшена
- Бесплатно, но есть ограничения (время жизни туннеля, скорость)

### 2. VPN с белым сервером (например, VPS за 3–5$/мес)

- Ты арендуешь **дешёвый VPS** с белым IP (DigitalOcean, Hetzner, Timeweb Cloud и т.п.)
- Поднимаешь там VPN (WireGuard, Tailscale)
- Твой домашний сервер подключается к этому VPS и через него становится доступен в интернете
- Твой напарник подключается к тому же VPN и попадает в твою сеть
- Работает как будто у вас общая локалка

### 3. Tailscale / Zerotier (виртуальная частная сеть, без VPS)

- Это P2P-VPN сервисы (часто бесплатно для личного использования)
- Ты ставишь Tailscale на свой домашний сервер и на ноутбук напарника
- Оба устройства получают IP из виртуальной сети (например, `100.x.x.x`) и видят друг друга напрямую
- Работает даже за серым NAT
- Очень удобно для командной разработки

### 4. GitHub Codespaces / Gitpod / Render (облачные dev-сервисы)

- Если приложение небольшое, можно развернуть его прямо в облаке (GitHub Codespaces или Gitpod)
- Тогда тебе даже не нужен свой сервер дома
- Но это уже больше «обходной путь», чем решение проблемы

### Рекомендация

- Если надо просто **потренироваться и дать доступ напарнику** → возьми **Tailscale** (бесплатный, простой, безопасный)
- Если потом будешь деплоить приложение публично → возьмёшь **VPS с белым IP** и развернёшь там Kubernetes

---

# Доступ через Tailscale

Если у домашнего сервера **нет белого IP**, то пробросы портов и NodePort не помогут извне. Для этого и пригодится **Tailscale**: это VPN-сервис, который создаёт mesh-сеть между твоими устройствами, даже за NAT.

## Цели настройки

1. Поднять Tailscale на домашнем сервере (где крутится k3s)
2. Подключить свой рабочий компьютер в эту же сеть
3. Через интерфейс приложения расшарить конкретный сервер с компаньоном

## Настройка Tailscale

### 1. Установка на домашнем сервере

На сервере (Ubuntu/Debian):

```bash
curl -fsSL https://tailscale.com/install.sh | sh
sudo tailscale up --ssh
```

> 💡 `--ssh` включает возможность подключаться по SSH через Tailscale (очень удобно, можешь забыть про порт-форвардинг роутера)

После команды тебе выведет ссылку в браузер для авторизации (через GitHub или Google). Подтверди — и сервер появится в твоей Tailscale-админке.

### 2. Установка на рабочем компьютере

- Скачай клиент [https://tailscale.com/download](https://tailscale.com/download) для своей ОС
- Установи и войди под тем же аккаунтом, что на сервере

Теперь твой рабочий комп и сервер будут в одной mesh-сети.

> ⚠️ **Важно**: Внутри аккаунта можно иметь доступ ко всем компьютерам сети, поэтому нужно давать доступ не через доступ к аккаунту, а через шаринг конкретных серверов.

### 3. Проверка подключения

```bash
tailscale status
```

На рабочем компьютере выполни:

```bash
tailscale ip -4
```

И на сервере:

```bash
tailscale ip -4
```

Каждое устройство получит **100.x.y.z адрес** (например `100.80.21.34`).
Теперь можно пинговать их друг друга:

```bash
ping 100.80.21.34   # IP сервера
```

### 4. Доступ к приложению (пока не работает. изучить)

Теперь у тебя есть прямой доступ по IP Tailscale сервера.

#### Если у тебя уже есть NodePort

Если у тебя уже есть `NodePort` (например `30080`), то приложение будет доступно по:

```
http://100.80.21.34:30080/swagger
```

#### Если NodePort не настроен

Можно просто пробросить порт на сервере через `kubectl port-forward`:

```bash
kubectl port-forward -n prod svc/tournament-service 8080:8080
```

Теперь приложение откроется по адресу:

```
http://100.80.21.34:8080/swagger
```

#### Создание сервиса для приложения

Если у тебя пока **нет сервиса для приложения**, только системный `kubernetes` (ClusterIP), то нужно создать сервис.

Например, `NodePort` (самый простой для твоего случая):

```yaml
apiVersion: v1
kind: Service
metadata:
  name: tournament-svc
  namespace: prod
spec:
  type: NodePort
  selector:
    app: tournament   # <- метка, должна совпадать с тем, что у pod/deployment
  ports:
    - protocol: TCP
      port: 8080        # порт сервиса (тот, что внутри pod)
      targetPort: 8080  # куда трафик пойдёт внутри pod
      nodePort: 30080   # внешний порт на сервере (доступный через Tailscale-IP)
```

Сохрани это в файл `service.yaml` и примени:

```bash
kubectl apply -f service.yaml
```

Проверить:

```bash
kubectl get svc -n prod
```

Должно появиться что-то вроде:

```
NAME            TYPE       CLUSTER-IP      EXTERNAL-IP   PORT(S)          AGE
tournament-svc  NodePort   10.43.125.11    <none>        8080:30080/TCP   10s
```

Теперь в браузере на своём компе (где Tailscale установлен) заходишь по адресу:

```
http://<tailscale-ip-сервера>:30080/swagger-ui/index.html
```

Например:

```
http://100.91.159.48:30080/swagger-ui/index.html
```

### 5. Дополнительные возможности

#### Tailscale Funnel (опционально)

Если захочешь, можно включить Tailscale Funnel, чтобы открыть приложение наружу в интернет с HTTPS без проброса портов. Но пока лучше ограничиться локальной mesh-сетью.

#### Выход из Tailscale

Чтобы выйти из Tailscale на сервере и на рабочем компе:

```bash
tailscale logout
```

Потом снова запустить авторизацию в новом аккаунте когда будет необходимо:

```bash
tailscale up
```

## Предоставление доступа коллеге

### Настройка SSH-доступа для коллеги

Чтобы коллега получил доступ к серверу через SSH:

1. **На сервере**: положить публичный ключ коллеги в `~/.ssh/authorized_keys`
2. **Коллега инициирует подключение**: `ssh user@server.com` (например, `ssh elena@100.87.128.106`)

### Как работает SSH-аутентификация по ключам

1. Сервер проверяет, есть ли у него ваш "замок" (публичный ключ). Он ищет его в файле `~/.ssh/authorized_keys` в домашней директории пользователя, под которым вы пытаетесь зайти.

2. Если ключ найден, сервер создает случайное сообщение (challenge) и шифрует его вашим публичным ключом. Расшифровать это сообщение можно только с помощью соответствующего приватного ключа.

3. Сервер отправляет это зашифрованное сообщение вашему клиенту.

4. Ваш SSH-клиент использует ваш приватный ключ (который он хранит локально, часто защищенный парольной фразой) чтобы расшифровать сообщение.

5. Расшифрованное сообщение отправляется обратно на сервер.

6. Сервер проверяет, совпадает ли полученный ответ с исходным сообщением. Если да — аутентификация успешна, и вам предоставляется доступ.
